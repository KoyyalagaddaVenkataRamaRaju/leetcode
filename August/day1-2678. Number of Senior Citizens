class Solution:
    def countSeniors(self, details: List[str]) -> int:
        total = 0
        for curr in details:
            if int (curr[11:13]) > 60:
                total += 1
        
        return total
